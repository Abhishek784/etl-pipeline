# ---- Core pipeline ----
pandas>=2.1,<3.0
numpy>=1.26,<3.0

# ---- Company name resolution ----
# rapidfuzz over fuzzywuzzy: MIT-licensed, no python-Levenshtein
# build step, and materially faster token_set_ratio.
rapidfuzz>=3.6,<4.0

# ---- Analytical store / hybrid search ----
duckdb>=1.0,<2.0

# ---- Semantic search (bonus section) ----
# sentence-transformers pulls torch + transformers. Install the
# CPU-only torch wheel first to avoid a ~2.5GB CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=3.0,<5.0

# ---- Columnar IO (DuckDB <-> pandas zero-copy, parquet) ----
pyarrow>=15.0

# ---- Testing ----
pytest>=8.0
pytest-cov>=5.0

# ---- Dev tooling (optional; not needed to run the pipeline) ----
# ruff>=0.6
# black>=24.0
